AGENT PREFLIGHT RESULT

This is architecture-sensitive.
Restart storm detected — do not patch local restart behavior first.

You must inspect:
- failure
- restart
- storm
- quorum
- maintenance
- recovery

Investigation order:
  1. Check restart singleflight gate (one restart per service per convergence tick)
  2. Inspect SIGTERM delivery and supervisor acknowledgement
  3. Verify start-limit reset is guarded (systemctl reset-failed before restart)
  4. Inspect convergence committer (workflow step that stamps CONVERGED)
  5. Verify desired → installed → runtime progression is not short-circuited

Warnings:
- NO_AWARENESS_MATCH: no awareness facts matched this task. This does not prove the task is safe.

This task is architecture-sensitive. Do not apply a local fix without checking the impacted invariants and forbidden fixes listed above. Restart storms in Globular must go through the singleflight restart gate — never restart a service directly from a convergence tick.
