# Staged release tarball — the build context's only Globular payload.
# (binaries/ units/ policy/ are gone: the image no longer assembles Globular
# from host copies, it installs the release. See Makefile RELEASE_VERSION.)
release/

# Frozen cluster state for fast scenario iteration (make snapshot).
snapshots/
.runlogs/
